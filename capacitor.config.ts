import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.lelinhtinh.scoreboard',
  appName: 'scoreboard',
  webDir: 'dist',
};

export default config;
